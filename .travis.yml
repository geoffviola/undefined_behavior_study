language: cpp
compiler: gcc
install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo add-apt-repository ppa:renemilk/llvm
  - sudo apt-get -qq update
  - sudo apt-get -qq install gcc-5 llvm-toolchain-3.8 valgrind
script: ./generate_results.bash
os: linux
